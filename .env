# Spring Boot API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Spring Boot Backend URL (for server-side calls if needed)
SPRING_BOOT_API_URL=http://localhost:8080/api

# Node Environment
NODE_ENV=development

#Ngrok


# Optional: If your Spring Boot backend requires specific headers or API keys
# API_KEY=your-api-key-if-needed
# CORS_ORIGIN=http://localhost:3000
